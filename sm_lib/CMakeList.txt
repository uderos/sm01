cmake_minimum_required (VERSION 2.6)
project (UdrSm)

# The version number.
set (UdrSm_VERSION_MAJOR 1)
set (UdrSm_VERSION_MINOR 0)
     
# configure a header file to pass some of the CMake settings
# to the source code
#configure_file (
#    "${PROJECT_SOURCE_DIR}/UdrSmConfig.h.in"
#    "${PROJECT_BINARY_DIR}/UdrSmConfig.h"
#)
     
# add the binary tree to the search path for include files
# so that we will find UdrSmConfig.h
#    include_directories("${PROJECT_BINARY_DIR}")
     
add_library(UdrSmLib tutorial.cxx)
